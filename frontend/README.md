# Frontend

React + Vite frontend for MecardStudio.

See the **[root README](../README.md)** for the full setup guide, project
structure, and troubleshooting.

## Quick start

```bash
npm install
npm run dev
```

The dev server proxies `/api/*` requests to the Django backend at
`http://127.0.0.1:8000`.
